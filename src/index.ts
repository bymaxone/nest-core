/**
 * @fileoverview Public barrel for the package root subpath. Ships the
 * dynamic module, the error-envelope exception filter, the request-timing
 * interceptor, DI tokens, shared interfaces, and the error-code catalog.
 * @layer public-api
 */

export {}
