/**
 * @fileoverview Public barrel for the `./pagination` subpath. Ships the
 * offset and cursor pagination DTOs, result builders, and the cursor codec.
 * Framework-neutral: no NestJS or ORM import in this subpath.
 * @layer public-api
 */

export {}
